# --- Core RAG stack ---
pypdf==5.1.0                 # PDF text + page extraction
sentence-transformers==3.3.1 # local embedding model (all-MiniLM-L6-v2)
chromadb>=1.0.0             # persistent vector store (prebuilt wheels, no C++ build needed)

# --- LLM provider (default: Gemini free tier) ---
google-generativeai>=0.8.0,<0.9.0
# Optional alternatives — install only if you switch LLM_PROVIDER:
# openai==1.57.4
# anthropic==0.42.0

# --- Interfaces ---
streamlit==1.41.1            # UI
fastapi==0.115.6             # API
uvicorn==0.34.0              # ASGI server for FastAPI

# --- Utilities ---
python-dotenv==1.0.1         # load API keys from .env
pydantic==2.10.4             # request/response models